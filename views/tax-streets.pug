include includes/head.pug
block content
    body
        .loader-bg
            .loader-track
                .loader-fill
        include includes/sidenav.pug
        include includes/nav.pug
        .pcoded-main-container
            .pcoded-wrapper
                .pcoded-content
                    .pcoded-inner-content
                        .main-body
                            .page-wrapper
                                p #[a(href="#" data-toggle="modal" data-target="#addAccount" class="btn btn-primary") #[i.feather.icon-plus] New]
                                div(class="modal fade" id="addAccount" role="dialog" aria-hidden="true")
                                    div(class="modal-dialog" role="document")
                                        div(class="modal-content")
                                            div(class="modal-header")
                                                h5 Create new Street
                                                button(class="close" type="button" data-dismiss="modal" aria-label="Close")
                                                    span(aria-hidden="true") ×
                                            div(class="modal-body")
                                                form(method="post")
                                                    label(for=" name") Name 
                                                    input(type="text" class="form-control mb-3" name="name" id="name" required) 
                                                    label(for=" lga") Zone
                                                    select(type="text" class="form-control mb-3" name="location" id="location" required) 
                                                        option(value="" selected disabled="disabled") Select Zone 
                                                        if locations.length > 0 
                                                            each location in locations 
                                                                option(value=location._id) #{location.name}
                                                    div(class="text-center")
                                                        input(type="submit", value="Proceed" class="btn btn-primary")
                                .row
                                    .col-sm-12
                                        .card
                                            .card-header
                                                h5 Streets
                                            .card-body
                                                if streets.length === 0 
                                                    h6 There is currently no street created
                                                else
                                                    .table-responsive
                                                        table.table.table-striped
                                                            thead 
                                                                tr 
                                                                    th S/N 
                                                                    th Name
                                                                    th Zone 
                                                                tbody 
                                                                    each street  , i in streets
                                                                        tr 
                                                                            td #{i + 1}
                                                                            td #{street.name} 
                                                                            td #{street.location.name}
                                                
        include includes/footer.pug